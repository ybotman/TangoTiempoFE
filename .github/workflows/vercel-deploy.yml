# This is a placeholder workflow for Vercel deployment
# Azure Static Web Apps deployment has been disabled

name: Deploy to Vercel

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Information
        run: echo "TangoTiempo.com is deployed via Vercel, not Azure Static Web Apps"