// /_app.js

/*
import React from 'react';
import PropTypes from 'prop-types';
import { LocalizationProvider } from '@mui/x-date-pickers/LocalizationProvider';
import { AdapterDateFns } from '@mui/x-date-pickers/AdapterDateFns';
import '../styles/globals.css';  // Assuming you have global styles

// Example of LocationsProvider, replace this with your actual implementation
const LocationsProvider = ({ children }) => {
    // Your logic to provide location-related context or state
    return (
        <div>
            {children}
        </div>
    );
};



function MyApp({ Component, pageProps }) {
    console.log('_app.js, MpApp, for Localizations');
    return (
        <LocalizationProvider dateAdapter={AdapterDateFns}>
            <LocationsProvider>
                <Component {...pageProps} />
            </LocationsProvider>
        </LocalizationProvider>
    );
}

MyApp.propTypes = {
    Component: PropTypes.elementType.isRequired,
    pageProps: PropTypes.object.isRequired,
};

export default MyApp;
import React from 'react';
import { LocalizationProvider } from '@mui/x-date-pickers/LocalizationProvider';
import { AdapterDateFns } from '@mui/x-date-pickers/AdapterDateFns';

function MyApp({ Component, pageProps }) {
    return (

        <LocalizationProvider dateAdapter={AdapterDateFns}>
            <Component {...pageProps} />
        </LocalizationProvider>
    );
}
*/