# JIRA Configuration
# Personal configuration for toby.balsley@gmail.com

JIRA_URL="https://tobybalsley.atlassian.net"
JIRA_EMAIL="toby.balsley@gmail.com"
PROJECT="TIEMPO"

# Token stored separately in keychain for security
# Run: security add-generic-password -a "$(whoami)" -s "jira-api-token" -w "your-token"